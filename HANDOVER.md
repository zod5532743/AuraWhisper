# AuraWhisper 開発引き継ぎ資料 (v1.0.5)

## 現在の状態
- **最新バージョン**: v1.0.5
- **配布用パス**: `C:\soft\AuraWhisper-v1.0.5-Portable`
- **開発用パス**: `d:\Users\zod5532743\マイドライブ\Antigravity\AuraWhisper`

## 完了した重要タスク
1.  **高解像度 (ウルトラワイド) 対応**:
    - `main.js` で `screen.getPrimaryDisplay()` を使用し、起動時に画面中央へ自動配置するロジックを実装。
2.  **ポータブル版の安定化**:
    - `node_modules` を同梱し、依存関係エラーを解消。
    - UIポートを `8240` に統一し、バックエンドとの通信を正常化。
3.  **クリーンアップ**:
    - `history.json`, `vocabulary.json` を空にし、`config.json` を初期化。
    - 個人データを含まない配布用パッケージとして整備。

## 注意事項
- バックエンドのポートは **8240** です。
- ポータブル版のルートにある `backend/setup.bat` を実行することで環境構築が完了します。
- ショートカットは `Alt + Shift + S` です。

## 次のステップ
- 必要に応じて、インストーラー形式へのパッケージングを検討。
- 各種 AI モデルのさらなる追加。
