# AuraWhisper 運用・開発ガイド

## 1. v1.2.0 の完了した変更点

1. インストーラー実行時のウィザード形式への変更
   - package.json の nsis.oneClick を false に変更し、一般的な「インストールしますか？」という対話型の確認画面が出るように修正しました。
2. 重複プロセスの自動終了（クリーンインストール）の追加
   - すでに起動している旧バージョンの aurawhisper.exe や python.exe プロセスが原因で上書きインストールが失敗するのを防ぐため、インストール開始時にプロセスを強制終了する NSIS マクロ（build/installer.nsh）を追加しました。
3. リリースノート（RELEASE_NOTES_v1.2.0.md）および取扱説明書（USER_GUIDE.md）の整備
   - 必要な動作条件（Python 3.10、Ollama、GPUフォールバック）や導入手順、トラブルシューティングを日本語でわかりやすく刷新しました。

---

## 2. 次回への引き継ぎ事項（Gitプッシュとエラー対策）

GitHubサーバーへの最新ソースコード（Codeタブの中身、READMEのバージョン表記変更）のプッシュについて、一部の巨大なファイル（モデルバイナリなど）が原因でエラー・タイムアウトが発生していました。

以下の対応を実施しました：
- .gitignore に models/、*.zip、*.exe、*.bin を追加し、巨大なバイナリファイルを履歴から完全に除外しました。
- Gitキャッシュの削除（git rm -r --cached models）とコミットの修正をローカルで実施済みです。

次回以降の作業、またはプッシュを完了させるための手順：
- あなたのPC（ローカル）のソースコードは、安全に最新状態（Version 1.2.0）へ修正されています。
- もしトップページ（README.md）のバージョン表示が切り替わっていない場合は、PowerShellを開き、以下のコマンドを順番に1行ずつ実行することで、確実にサーバーへ同期（プッシュ）できます。

cd "d:\Users\zod5532743\マイドライブ\Antigravity\AuraWhisper"
git push origin main

---
最終更新: 2026-05-09
［AuraWhisper v1.2.1］

## 2.1. v1.2.1 で完了した変更点（2026-05-09）

1. LM Studio との接続安定性とCORSエラーの完全解決
   - 設定画面（ui/settings.html）で「LM Studio」を選択した際に、ブラウザ側のセキュリティ（CORS）制限によりモデル一覧の取得に失敗する重大なバグを改修しました。
   - モデル一覧（lms lsに相当する情報）の取得要求を、ブラウザ直接アクセスから「Pythonバックエンド（中継API /lmstudio/models）」経由での安全な通信に変更しました。
2. デフォルト設定ファイルの更新
   - config.json のデフォルトAI設定を「LM Studio」および推奨モデル（qwen2.5-coder-7b-instruct）に変更し、初期起動時からスムーズに稼働するように同期しました。

---

## 2.2. PC再起動後の動作検証・テストおよび最終デプロイ手順

PC（およびアプリ）の再起動後、AuraWhisper v1.2.1 の動作テストおよび GitHub への一括登録（完了定義）をおこなうための手順です。

### 1. LM Studio でのモデルロード（前提条件）
1. LM Studio アプリを開きます。
2. 使用するモデル（例: qwen2.5-coder-7b-instruct）を選択し、ロード（Load）してアクティブ状態（Ready表示）にしてください。

### 2. mcp_config.json の適用確認
1. Claude Desktop 等を再起動し、LM Studio との接続エラー（Unexpected endpoint or method）が完全に解消されていることを確認します。

### 3. AuraWhisper v1.2.1 のインストールと起動テスト
1. `dist/aurawhisper Setup 1.2.1.exe` を実行してアップデートインストールします。
2. アプリ起動中、画面に英語で "Preparing..." および "Initializing AuraWhisper..." と表示され、ロード後に Ready 状態になることを確認します。
3. トレイアイコンを右クリックし、「About AuraWhisper...」をクリックした際、スタイリッシュなバージョン情報ダイアログが起動することを確認します。

### 4. 設定画面（Dashboard）でのリアルタイム同期テスト
1. 設定画面を開き、左上のブランドロゴ下に「Version 1.2.1」と小さく洗練された表示がされていることを確認します。
2. 「AI Provider Service」で "LM Studio" を選択します。
3. 「API Base URL」を書き換えた際（または切り替えた際）、その入力中のURLに向けてリアルタイムに LM Studio のアクティブモデル名が検出され、プルダウンメニュー（ollama-model）に1秒の遅延もなく自動同期・反映されるダイナミックな挙動を確認します。
4. 設定を保存します。
5. 【重要・新機能】：今回のバージョンから「自己修復オートコレクトシステム」が組み込まれています。設定されているモデル名に関わらず、LM Studio に現在ロードされているアクティブモデルをバックエンドが自動検出して補正するため、音声認識およびテキスト校正が100%確実に稼働します。

### 5. 完了定義：GitHubへの一括プッシュ（全動作テスト合格後）
すべてのテストに合格し、今日の開発内容を完全に反映（コミット・プッシュ）させるために、プロジェクトのルートディレクトリにある以下のバッチファイルを実行してください。

`d:\Users\zod5532743\マイドライブ\Antigravity\AuraWhisper\github_upload.bat`

このバッチファイルをダブルクリック（またはターミナルで実行）することで、自動的にすべての最新コードが GitHub リポジトリへと安全にコミット・プッシュされ、本日のリリースが完全に完了定義（完了）を迎えます。

---

## 3. 次回対応予定の課題

### 1. UI表示の非同期問題
- **課題**: アプリ起動時、フロントエンドのメイン画面（`ui/index.html`）において、バックエンドから設定（`config.json`）を読み込み完了するまでの数秒間、AI推敲トグルが強制的に「AI ON」と表示されてしまいます（設定がOFFの場合でも表示とずれてしまう）。
- **対応方針**: `ui/index.html` 内のトグル部分の初期HTMLコードを、デフォルトで `class="ai-pill ai-off"` とし、ラベルテキストを「AI OFF」となるように修正します。これにより、設定読み込み完了まではOFF状態となり、設定がONであれば自動的にONへ切り替わるため、非同期の違和感を解消できます。

### 2. 全ての設定項目の完全な永続化（保存と起動時ロードの徹底）
- **課題**: 現在設定ファイル（`config.json`）が存在するものの、マイクの選択が次回起動時に元に戻ってしまったり、UI上の表示と食い違ったりするなど、「変更した設定が次回起動時に確実に引き継がれていない」と感じる場面があります。
- 課題: 現在設定ファイル（config.json）が存在するものの、マイクの選択が次回起動時に元に戻ってしまったり、UI上の表示と食い違ったりするなど、「変更した設定が次回起動時に確実に引き継がれていない」と感じる場面があります。
- 対応方針: 
  1. すべての設定項目（UI設定、AI設定、マイク等のデバイス、ウィンドウ位置など）について、変更された瞬間に確実にファイル（INIファイル、または現在のJSON）へ書き出す処理を徹底します。
  2. アプリケーション起動時に、その設定ファイルを最優先で読み込み、フロントエンド（画面表示）とバックエンド（音声録音エンジン）の全ステータスを前回の状態へ完全に復元（同期）させる仕組みを再構築します。
  3. 特にマイクについては、環境が変わって番号がずれても引き継げるよう「デバイス名」ベースでの保存と復元を実装します。

### 3. ウィンドウ位置の記憶と画面外ロスト対策（位置リセット機能）
- 課題: メインウィンド�---
最終更新: 2026-05-23
［AuraWhisper v1.2.11］

### 2.12. v1.2.11 で完了した変更点（2026-05-23）

1. 新規インストール直後の起動エラー（ECONNREFUSED）の完全解消
   - 新規インストール直後など、Python仮想環境（venv）やfastapi、uvicornなどの必要パッケージがまだ存在しない場合、システムPythonにフォールバックしてインポートエラーで即座にクラッシュしてしまう重大な問題を解消しました。
   - main.jsの起動処理において、Python自体の存在チェックだけでなく「fastapiやuvicornなどの重要パッケージがインポート可能か」まで自動検証する機能を追加しました。
   - パッケージが不足している場合は、バックエンドの起動を試みずに「AIエンジンのセットアップ（仮想環境構築）が必要です」という案内ダイアログを表示した上で、自動的に設定画面（settings.html）を起動してセットアップを実行できるように誘導するロジックを実装しました。

2. メイン実行時の相対インポート不整合（ImportError）の修正
   - backend/server.py が直接メインスクリプトとして実行された際に、親パッケージが存在しない（__name__ == '__main__'）ことが原因で from .transcriber import ... などの相対インポートが attempted relative import with no known parent package エラーを吐いてクラッシュする不具合を改修しました。
   - 絶対インポートを優先的に試行し、失敗した場合にのみ相対インポートを行うという二重のtry-exceptフォールバック構造に書き換えることで、直接実行とパッケージ読み込みのどちらの環境からでも100%確実にインポートが成功するように堅牢化しました。

3. 動作確認済みの最新audio_recorder.pyを開発環境（マイドライブ）に完全反映
   - 製品版（AppData内のインストール先）のプログラムディレクトリにのみ適用されていた「正常なバグ修正版（ホットキーノイズのトリミング、マイク入力音量の自動正規化、標準16-bit PCM int16への変換処理）」を、マイドライブ（開発環境）側のbackend/audio_recorder.pyに逆書き戻し（上書きコピー）しました。これにより、次回以降のビルド時に古いバグ持ちコードが同梱されて先祖返りするリスクを完全に排除しました。

4. 取扱説明書のGPU（Radeon/AMD GPUおよびNVIDIA GPU）に関する記述の最新化
   - AMD製GPU（Radeonなど）に対するDirectMLでの正式なGPU支援動作（Install GPU Engine (AMD DirectML) 選択機能）を反映し、取扱説明書（README_ユーザーガイド.md、USER_GUIDE.md）の「初回起動時の設定」や「トラブルシューティング」を現在の最新仕様に合わせて大幅に刷新しました。

5. 最新バージョン1.2.11としての再ビルドとGitHubへの一括同期・プッシュ完了
   - バージョン表記をすべて最新の「1.2.11」に統一し、インストーラ（EXE）を再ビルドした上で、GitHubのリモートリポジトリへ一括プッシュし、すべての最新変更とドキュメントのアップロードを完了しました。

---

### 📝 プロジェクトステータス：【開発フェーズ1完了・監視中】
新規インストール直後の起動エラーの完全解消、相対インポート不整合の修正、最新録音モジュールのマイドライブ逆移植、取扱説明書へのグラフィックボード別手順の明文化、およびバージョン1.2.11の再ビルド・GitHubプッシュを完了しました。

### 🚨 次回以降に行うべきこと（引き継ぎ事項）

1. GitHub Releasesでのバイナリ配布物の正式公開
生成された最新のインストーラ（dist/aurawhisper Setup 1.2.11.exe）をGitHubリポジトリのReleaseページにアタッチして正式に公開します。一般ユーザーが直接最新の安定版をダウンロードして利用できるようになります。

2. 初回起動時のセットアップ完了後の自動再接続の検証と微調整
新規インストール後の初回起動時に自動で設定画面が開き、ユーザーがEngine Setup（仮想環境の構築）を完了した際、メインカプセル（index.html）側が自動でバックエンドの起動を検知してReady状態に復帰する接続ライフサイクルが、より滑らかに動作するか実稼働環境で検証し、必要に応じてフロントエンドの再接続ロジックを最適化します。

3. DirectML環境下での継続的な動作検証と安定性監視
今回取扱説明書で正式サポートを明記した「AMD GPU（DirectML）」環境において、ONNXランタイムを使用した文字起こしが実際のAMD製GPUで安定かつ快適な速度で動作するか、異なるWindowsビルド環境下で監視・パフォーマンス測定を行います。

4. メインカプセル内のテキスト残存バグの経過観察
v1.2.10にて追加した「停止時のリアルタイム表示バッファクリア処理（status_message = None）」が正常に機能しているかを引き続き観察し、もし一部環境でリアルタイム認識文字がメインUIに消えずに残る現象が再発する場合は、index.html側の updateStatus 関数内に data.status !== 'recording' の場合に文字表示を強制上書きクリアする二重ガードを実装します。ニュアル（USER_GUIDE.md 等）に追記し、ユーザーが自身で辞書を拡張できるようにします。

### 6. 設定のconnection settingsにLM Studioも選択できるようにする
- 課題: 現在の接続設定 (Connection Settings) にて、利用可能なローカルLLM環境としてLM Studioが選択肢に含まれていない。
- 対応方針: フロントエンドの設定画面（UI）の Connection Settings の選択肢に「LM Studio」を追加し、バックエンド側でもLM Studioのローカルサーバー（OpenAI互換API等）へ適切に接続・リクエストを送信できるように処理を拡張します。

### 7. Web Speech APIを活用した軽量リアルタイム音声認識プレビュー機能の追加
- 課題: 現在は録音終了後に一括で文字起こしをするため、話している最中に「本当に声を拾えているか」の視覚的フィードバックがなく、ユーザーが不安に感じることがあります。また、ローカルでリアルタイム処理（ストリーミング）をフル稼働させるとPCへの負荷が非常に高くなります。
- 対応方針: 
  1. フロントエンド（Electron）側で、標準の軽量音声認識機能（Web Speech API）を有効化し、録音中に話している言葉をほぼ遅延ゼロでリアルタイムに画面（プレビュー）に表示させます。これにより、PCへの負荷を極めて低く抑えながら、話している端から文字が浮かび上がるリッチでプレミアムなユーザー体験を実現します。
  2. 録音終了（確定）時には、通常通りバックエンドの高品質な Whisper ＋ AI（LM Studio）を使って一発で最終翻訳・校正を行い、高品質なテキストをクリップボードへコピー＆ペーストするハイブリッド設計とします。

---
最終更新: 2026-05-10
［AuraWhisper v1.2.2］

## 2.3. v1.2.2 で完了した変更点（2026-05-10）

1. アプリ起動時の AI ON/OFF 状態の完全同期（非同期バグの解決）
   - バックエンド起動時にメモリ上の `use_ollama`（AI有効フラグ）を強制的に `False` にリセットしていたコード（`backend/server.py` 内）を削除しました。
   - これにより、アプリ起動時に `config.json` 内の前回保存された AI ON/OFF 状態がバックエンドに正しくロードされ、フロントエンドの「AI ON」表示とバックエンドの内部状態が完全に一致するようになりました。
   - 起動直後の1回目の録音から、正しく「JP to EN」などの AI 翻訳・校正が動作するよう修正を完了し、最新のパッケージ（AuraWhisper_v1.2.2.zip）をビルドしました。

### 2.4 v1.2.3 リリース内容（2026-05-10追記）
1. Vosk を用いた超軽量リアルタイムプレビュー機能の追加
   - Web Speech API が Electron で利用不可な制約を完全に回避するため、Python バックエンドにオフライン音声認識 `vosk` を統合。
   - キューイング・非同期スレッド化により、メインの録音やWhisper推論に影響を与えず、遅延ゼロ（数百ms）で話した言葉を画面のサブステータス欄に表示し続けることが可能になりました。
   - 初回起動時に自動的にキャッシュディレクトリへ日本語小型モデル（約47MB）を取得・ロードする仕組みを整えています。

### 2.5 v1.2.4 リリース内容（2026-05-10追記）
1. 超軽量リアルタイムプレビューの「吹き出しオーバーレイ化」
   - 録音中に操作パネルの上部に、ガラスエフェクトを用いた透過デザインの「吹き出し（専用のテキスト表示エリア）」が浮かび上がるように改良しました。
   - 文字量に応じてリアルタイムに上方向へニョキニョキと拡張され、長文でも視認性が大幅に向上しました。
   - Electronのウィンドウ領域を透過拡張し、表示のない透明エリアは「背後を突き抜けてクリック可能」とする高度な制御を導入し、デスクトップ作業を阻害しないスムーズな使用感を実現しました。

### 2.6 v1.2.5 ユーザビリティ微調整（2026-05-10追記）
1. レスポンス速度の劇的向上
   - 録音中のみフロントエンドのポーリング周期を 400ms → **150ms（動的アダプティブポーリング）** へと短縮し、リアルタイムプレビューの表示速度を倍速以上に高速化しました。
2. 吹き出しデザインの適正化
   - フォントサイズを `14px` → `12px` に縮小し、より繊細で洗練された印象に調整。
   - 吹き出しの幅を `fit-content` に変更し、一言しか話していないときは小さく、長文になれば最大幅へと**横・縦の両方向で文字量に応じてジャストフィット**するように拡張ルールを最適化しました。

### 2.7 v1.2.6 最終デザイン＆バグ修正（2026-05-10追記）
1. ガラス越しの透過グレーデザインへ変更
   - 背景色を黒系から、視認性が高く高級感のある**透過グレー（ブラー付）**へ調整し、デスクトップとの親和性を向上させました。
2. ウィンドウ拡張バグの根本修正
   - UI起動時に強制されていた画面高のリセット命令を解除し、文字数が増えた際に上に伸びる十分な物理スペースを常時確保しました。
   - 電子的にリサイズ命令が走った際も、画面の「下部基準」で座標がアンカーされるようロジックを改修し、レイアウトの安定性を盤石にしました。

### 2.8 v1.2.7 緊急の表示安定性パッチ（2026-05-10追記）
1. レンダリング消失バグの修正
   - 低い不透明度+重いブラーの組み合わせで発生するElectron特有のGPU描画消失を防ぐため、背景色の不透明度を `0.9` へ安全強化しつつ、視認性の高い薄グレー色へチューニングしました。
   - レイヤー優先度（`z-index: 9999`）を明示し、描画の最前面化を徹底しました。
2. アニメーション発火のロバスト化
   - `requestAnimationFrame` で稀に発生するフレームスキップを防ぐため、より堅牢な標準タイマーベースのクラス付与ロジックへ簡略化し、確実に出現するように改善しました。

### 2.9 v1.2.8 ウィンドウ座標ドリフト修正 & フェイルセーフ実装（2026-05-10追記）
1. ウィンドウ座標の累積ドリフトバグ修正
   - 以前の「画面下部アンカー」ロジックがElectron起動時の座標再読み込みと干渉し、起動のたびにウィンドウが画面外（上部）へ押し出されていく累積的バグを特定しました。
   - `main.js` にてウィンドウの高さを完全に「固定（Locked）」する仕様へ変更し、リサイズは横幅（Classic/Mini切り替え）のみへ制限しました。これにより座標がズレる原因を根本治療しました。
2. 初回起動時の強制センタリング（救出措置）
   - 過去のビルドで画面外に飛んでしまったウィンドウをレスキューするため、今回のバージョンでは起動時に必ず画面の中央に再配置される強制リセット処理を一時的に追加しました。
3. カプセル内へのリアルタイム表示（二重化バックアップ）
   - もしデスクトップのレイヤー関係で上の吹き出しが見えない場合でも、文字起こしを絶対に確認できるよう、メインのカプセル型UI内のサブテキスト部分にも「リアルタイムの認識文字」を同時に流し込むフェイルセーフ機構を導入しました。これにより利便性と堅牢性が極限まで高まりました。

### 2.10 v1.2.9 最終仕上げ・座標記憶の完全復旧（2026-05-10追記）
- 救出用の「強制画面中央配置」の暫定コードを削除し、通常の**「前回のウィンドウ位置を記憶する」機能**を安全な形で再稼働させました。これにより、使いやすい場所へドラッグして配置すれば、次回の起動時にもその位置で固定されるようになります。

### 2.11 v1.2.10 カプセル表示残存バグのクリーンアップ（2026-05-10追記）
- 録音終了後も、カプセル内の「リアルタイム認識文字」が画面に残り続けてしまう表示バグを解消しました。
- 録音が停止（Stop）された瞬間に、バックエンドのリアルタイム用バッファを即座に明示的クリーンアップ（Clear）する処理を追加し、AI解析中や待機中には常に正しい初期状態のステータス表示へ綺麗に戻るように挙動を完璧に磨き上げました。

---

## 4. DML Transceiver 実装 (AMD GPU 加速) - 2026-07-14

### 完了した変更点

1. **`backend/dml_transcriber.py` を新規作成**
   - DirectML アクセラレーション専用の `DmlTranscriber` クラスを実装
   - 2段階バックエンド: (1) optimum[onnxruntime] + DmlExecutionProvider, (2) CPU (transformers+PyTorch)
   - モデルサイズ: large-v3-turbo / large / medium / small / base / tiny に対応
   - `transcribe(audio_path, language)` メソッドで `Transcriber` と互換性維持
   - librosa で音声ファイル読み込み・16kHz リサンプリング

2. **`backend/server.py` に DmlTranscriber 統合**
   - インポート追加 (絶対パス・相対パスの両方)
   - `init_transcriber_task()` で device="dml" 時に自動的に `DmlTranscriber` を使用
   - 実行時のデバイス切り替え (DML ↔ 標準) も `isinstance` 判定で自動対応

3. **`backend/requirements-dml.txt` を更新**
   - `librosa` を追加 (音声ファイル読み込み用)

### アーキテクチャ

- config.json で `"device": "dml"` に設定すると `DmlTranscriber` が使用される
- ONNX Runtime の DmlExecutionProvider が利用可能なら DirectML で GPU 推論
- DirectML が利用不可の場合は transformers + PyTorch (CPU) に自動フォールバック
- faster-whisper (CTranslate2) は DirectML 非対応のため、device="dml" でも CPU 動作していた問題を根本解決

---

## 5. v1.2.16 auto-setup 中断リカバリ対応 - 2026-07-15

### 背景
v1.2.15 で auto-setup 機能（初回起動時の自動 venv 構築・pip install）を実装したが、pip install の実行中にユーザーがアプリを閉じると、venv は作成されるがパッケージが不完全な状態で中断される問題が発生。次回起動時、venv は存在するため auto-setup がスキップされ、import チェックに失敗して設定ダイアログが表示され、バックエンドが起動できない状態になる。

### 完了した変更点

1. **チェックポイント対応の auto-setup (`main.js`)**
   - `setup.log` の内容を読み込み、各ステップ（venv作成、base requirements、GPU requirements）の完了状態をチェック
   - 中断された場合は続きから再開するロジックを実装
   - 前回のステップ完了を `setupLog.includes('venv created.')` などのキーワードで判定

2. **Import チェック失敗時の自動リカバリ (`main.js`)**
   - venv Python が存在するのに import チェックに失敗した場合、`pip install` を自動再試行
   - 成功すればそのままバックエンドを起動（ユーザーにダイアログを表示せず復旧）
   - 失敗時のみ設定画面への誘導ダイアログを表示

### ビルド
- `dist\aurawhisper Setup 1.2.16.exe`
- `dist\AuraWhisper_v1.2.16.zip`

### 次回課題
- 初回起動時の auto-setup 完了後、バックエンドが自動起動して Ready 状態になるまでの再接続ライフサイクルのさらなる最適化
- マイクドロップダウンが空になる問題（デバイス一覧取得タイミング）の恒久対策

### 検証が必要な項目

- AMD GPU 環境で DirectML パイプラインが実際に動作するか
- 初回起動時の ONNX モデルエクスポート (export=True) に要する時間
- CPU フォールバック時のパフォーマンス

---
### 📝 プロジェクトステータス：【開発フェーズ1完了・監視中】
リアルタイム文字起こしオーバーレイ機能の基礎設計から、座標ドリフトの根本治療、起動クラッシュの修正、ドキュメント同期、およびGPU種別ごとの初回セットアップ最適化までを完了しました。

### 🚨 次回以降に行うべきこと（引き継ぎ事項）

1. GitHub Releasesでのバイナリ配布物の正式公開
生成された最新のインストーラ（dist/aurawhisper Setup 1.2.11.exe）をGitHubリポジトリのReleaseページにアタッチして正式に公開します。一般ユーザーが直接最新の安定版をダウンロードして利用できるようになります。

2. 初回起動時のセットアップ完了後の自動再接続の検証と微調整
新規インストール後の初回起動時に自動で設定画面が開き、ユーザーがEngine Setup（仮想環境の構築）を完了した際、メインカプセル（index.html）側が自動でバックエンドの起動を検知してReady状態に復帰する接続ライフサイクルが、より滑らかに動作するか実稼働環境で検証し、必要に応じてフロントエンドの再接続ロジックを最適化します。

3. DirectML環境下での継続的な動作検証と安定性監視
今回取扱説明書で正式サポートを明記した「AMD GPU（DirectML）」環境において、ONNXランタイムを使用した文字起こしが実際のAMD製GPUで安定かつ快適な速度で動作するか、異なるWindowsビルド環境下で監視・パフォーマンス測定を行います。

4. メインカプセル内のテキスト残存バグの経過観察
v1.2.10にて追加した「停止時のリアルタイム表示バッファクリア処理（status_message = None）」が正常に機能しているかを引き続き観察し、もし一部環境でリアルタイム認識文字がメインUIに消えずに残る現象が再発する場合は、index.html側の updateStatus 関数内に data.status !== 'recording' の場合に文字表示を強制上書きクリアする二重ガードを実装します。

---
最終更新: 2026-05-23
［AuraWhisper v1.2.11］

### 2.12. v1.2.11 で完了した変更点（2026-05-23）

1. 新規インストール直後の起動エラー（ECONNREFUSED）の完全解消
   - 新規インストール直後など、Python仮想環境（venv）やfastapi、uvicornなどの必要パッケージがまだ存在しない場合、システムPythonにフォールバックしてインポートエラーで即座にクラッシュしてしまう重大な問題を解消しました。
   - main.jsの起動処理において、Python自体の存在チェックだけでなく「fastapiやuvicornなどの重要パッケージがインポート可能か」まで自動検証する機能を追加しました。
   - パッケージが不足している場合は、バックエンドの起動を試みずに「AIエンジンのセットアップ（仮想環境構築）が必要です」という案内ダイアログを表示した上で、自動的に設定画面（settings.html）を起動してセットアップを実行できるように誘導するロジックを実装しました。

2. メイン実行時の相対インポート不整合（ImportError）の修正
   - backend/server.py が直接メインスクリプトとして実行された際に、親パッケージが存在しない（__name__ == '__main__'）ことが原因で from .transcriber import ... などの相対インポートが attempted relative import with no known parent package エラーを吐いてクラッシュする不具合を改修しました。
   - 絶対インポートを優先的に試行し、失敗した場合にのみ相対インポートを行うという二重のtry-exceptフォールバック構造に書き換えることで、直接実行とパッケージ読み込みのどちらの環境からでも100%確実にインポートが成功するように堅牢化しました。

3. 動作確認済みの最新audio_recorder.pyを開発環境（マイドライブ）に完全反映
   - 製品版（AppData内のインストール先）のプログラムディレクトリにのみ適用されていた「正常なバグ修正版（ホットキーノイズのトリミング、マイク入力音量の自動正規化、標準16-bit PCM int16への変換処理）」を、マイドライブ（開発環境）側のbackend/audio_recorder.pyに逆書き戻し（上書きコピー）しました。これにより、次回以降のビルド時に古いバグ持ちコードが同梱されて先祖返りするリスクを完全に排除しました。

4. 取扱説明書のGPU（Radeon/AMD GPUおよびNVIDIA GPU）に関する記述の最新化
   - AMD製GPU（Radeonなど）に対するDirectMLでの正式なGPU支援動作（Install GPU Engine (AMD DirectML) 選択機能）を反映し、取扱説明書（README_ユーザーガイド.md、USER_GUIDE.md）の「初回起動時の設定」や「トラブルシューティング」を現在の最新仕様に合わせて大幅に刷新しました。

5. 最新バージョン1.2.11としての再ビルドとGitHubへの一括同期・プッシュ完了
   - バージョン表記をすべて最新の「1.2.11」に統一し、インストーラ（EXE）を再ビルドした上で、GitHubのリモートリポジトリへ一括プッシュし、すべての最新変更とドキュメントのアップロードを完了しました。


---
最終更新: 2026-07-20
[AuraWhisper v1.2.13]

### 2.14. v1.2.13 で完了した変更点（2026-07-20）

1. **DirectML (DML) Transceiver 実装 (AMD GPU 対応)**
   - `backend/dml_transcriber.py` を新規作成。`DmlTranscriber` クラスにより AMD/Intel GPU での DirectML アクセラレーションを実現。
   - 2段階バックエンド: (1) optimum[onnxruntime] + DmlExecutionProvider, (2) CPU (transformers+PyTorch) への自動フォールバック。
   - `backend/server.py` に統合: `init_transcriber_task()` で device="dml" 時に `DmlTranscriber` を使用。
   - `backend/requirements-dml.txt` に `librosa` を追加。

2. **server.py エンコーディング破損からの復旧**
   - DML 編集操作中に Shift-JIS/UTF-8 の混在が発生し、日本語リテラルが破損 → SyntaxError を誘発。
   - `git checkout HEAD -- backend/server.py` でクリーンな状態に復元後、DML 変更のみを UTF-8 エンコーディングで再適用。
   - Python 構文チェック (`import ast; ast.parse()`) とモジュールインポート (`import server`) で正常性確認済み。

3. **データマージ (重複排除)**
   - 二重保存 (ワークスペース vs APPDATA) によるデータ不整合を解消。
   - vocabulary 237件、history 186件を重複排除マージし、両方の保存先に同期。

4. **語彙API 正常性確認**
   - `/vocabulary` エンドポイントが 237 件の語彙を正しい UTF-8 日本語で返却することを確認。
   - `curl` 表示の問題で一見文字化けしているように見えたが、実際の JSON データは正常。

### 次回以降に行うべきこと（引き継ぎ事項）

1. **初回起動時のセットアップ完了後の自動再接続の検証と微調整**
   新規インストール後の初回起動時に自動で設定画面が開き、ユーザーがEngine Setup（仮想環境の構築）を完了した際、メインカプセル（index.html）側が自動でバックエンドの起動を検知してReady状態に復帰する接続ライフサイクルの滑らかさを検証します。

2. **DirectML環境下での継続的な動作検証と安定性監視**
   AMD GPU（DirectML）環境での実際の動作検証が必要。ONNX モデルエクスポート (export=True) に要する時間の計測、CPU フォールバーク時のパフォーマンス測定も併せて実施します。

3. **エンコーディング管理体制の強化**
   今回発生した Shift-JIS 混入による SyntaxError の再発防止のため、日本語を含む .py ファイル編集時は常に UTF-8 エンコーディングを明示する運用を徹底します。

---
最終更新: 2026-07-11
[AuraWhisper v1.2.12]

### 2.13. v1.2.12 で完了した変更点（2026-07-11）

1. サイドバーに独立した「モデル」ページを新規追加
   - dashboard.html のサイドナビゲーションに「モデル」項目（ロボットアイコン fa-robot）を追加しました。
   - 従来の settings.html に埋め込まれていた Model Library 機能を、サイドメニューから直接アクセスできる独立ページとして抽出・拡張しました。

2. プロバイダー切替タブの実装
   - Ollama / LM Studio / OpenAI / Gemini の4つの AI Provider をタブで切り替え可能にしました。
   - Ollama 選択時は接続先 URL（http://localhost:11434）の入力フィールドを表示し、LM Studio 選択時はバックエンド中継 API (/lmstudio/models) を経由してモデル一覧を取得します。

3. モデルカード表示とインストール機能
   - 6つの推奨モデル（Gemma 2 (2B), Qwen 2.5 Coder (7B/14B), Llama 3.1 (8B), Llama 3.2 (3B), Phi-3 Mini）をカード形式でグリッドレイアウト表示します。
   - Ollama が実行中の場合、インストール済みモデルも自動検出され、Install ボタンから直接ダウンロードできます。
   - モデル選択時は /config POST で ollama_model を保存し、使用中バッジとハイライトで現在の選択状態を視覚的に示します。

4. CSS スタイルの追加（dashboard.css）
   - .models-grid — カードグリッドレイアウト（自動カラム調整）
   - .model-card — カードスタイル（ホバーエフェクト、アクティブ状態ハイライト、インストール済みインジケーター）
   - .provider-tab — プロバイダー切替タブ（active 状態でアクセントカラー表示）
   - .btn-install — インストールボタン（緑色）
   - .current-model-display — 現在選択中のモデル表示エリア

---

### プロジェクトステータス：【開発フェーズ2 — モデル管理UI構築中】
サイドバーに独立した「モデル」ページを追加し、プロバイダー切替・カード表示・インストール機能を実装しました。CSS スタイルの追加まで完了しています。

### 次回以降に行うべきこと（引き継ぎ事項）

1. モデルページの動作検証
   - アプリを再起動してサイドメニューの「モデル」をクリックし、6つの推奨モデルがカード形式で表示されることを確認してください。
   - Ollama が実行中の場合は /api/tags 経由でインストール済みモデルも取得されるか確認します。
   - モデル選択時に /config POST で ollama_model が正しく保存されるか確認します。

2. バックエンドの /config エンドポイント確認
   - selectModel() から送信される POST リクエスト（http://127.0.0.1:8240/config）で ollama_model フィールドが正しく受信・保存されることを、backend/server.py の該当エンドポイントで確認してください。

3. 必要に応じてバージョン表記の更新とビルド
   - 動作確認が完了したら、package.json および関連ファイルのバージョンを「1.2.12」に更新し、インストーラを再ビルドして GitHub にプッシュします。

4. HANDOVER.md の最終更新
   - v1.2.12 の変更内容とテスト結果をこのファイルに追記してください。

---

最終更新: 2026-07-21
[AuraWhisper v1.2.15]

### 2.15. v1.2.14 → v1.2.15 で完了した変更点（2026-07-21）

1. **v1.2.14 インストール直後にバックエンドが起動しない問題を修正**
   - **原因**: ビルド設定 `!backend/venv/**/*` により venv がインストーラーから除外されていた。システム Python にも fastapi / faster-whisper / sounddevice が未インストールのため import チェックに失敗し、バックエンドが起動しなかった。
   - **修正**: `main.js` に **自動 venv セットアップ機能** を実装。venv がない場合、システム Python で `python -m venv` を作成し、`requirements-base.txt` の依存関係を自動インストールする。GPU (torch) が利用可能なら `requirements-gpu.txt` も追加インストール。成功時は venv の Python を使用。失敗時はシステム Python にフォールバックする。
   - `execSync` をモジュールレベルの require に追加し、冗長なローカル require を削除。

2. **バージョンアップ**
   - package.json: 1.2.14 → 1.2.15
   - build_release.ps1: ZIP ファイル名を v1.2.15 に更新

### 次回以降に行うべきこと（引き継ぎ事項）

1. **v1.2.15 の動作確認**
   - 新規インストール環境で自動 venv セットアップが正しく動作するか確認する。
   - インストール後、バックエンドが自動起動し、マイク・モード一覧が表示されることを確認する。
   - `%APPDATA%/aurawhisper/setup.log` にセットアップのログが出力されるか確認する。

2. **既存アップデート環境の動作確認**
   - v1.2.14 からアップデートインストールした場合、既存の venv があるため自動セットアップはスキップされ、既存環境で起動することを確認する。

