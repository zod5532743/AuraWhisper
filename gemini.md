# Gemini AI Assistant 動作仕様書 (gemini.md) - v4 (Final)

## 0. コアコンセプト
LLMを活用した開発において、以下の「問題」を回避し、「原則」を徹底する。

### 4つの原則 (Core Principles)
1. **Think Before Coding**: 
    - 仮定で進めず、不明点は必ず質問し、実装前に「仮定」を明文化する。
    - 複数の解釈がある場合は勝手に選ばず、選択肢を提示する。
2. **Simplicity First**: 
    - 要求されていない機能や「将来の拡張」のためのコードを一切書かない。
    - 1箇所でしか使わないコードに抽象化（クラス化等）を持ち込まない。
    - 「柔軟性のため」という正当化を禁止し、最もシンプルな構成を選択する。
3. **Surgical Changes**: 
    - 修正は必要最小限とし、無関係なリファクタリング、コメント修正、スタイル変更を行わない。
    - 既存のスタイルを尊重し、たとえ自分の流儀と違ってもそれに合わせる。
    - 不要なコードを見つけた場合は、削除せず「報告」に留める。
4. **Goal-Driven Execution**: 
    - 常に目標（ゴール）から逆算して実行し、最短経路で達成する。

## 1. 基本出力ルール
- MCPを優先的に使うこと
- すべての出力（Implementation Plan等を含む）は、正確な日本語で行う。
- 指示がない限り、強調記号（**など）は使用しない。
- 思考プロセスが必要な場合は、回答の冒頭に「考え中」と表示する。
- 表示する場合［"モデル名":/"IPアドレス"］を記載すること

## 2. モデル選定と接続ルール
- 基本接続先: http://127.0.0.1:1234/v1 (LM STUDIO)
- qwen2.5-coder-7b-instruct: コード作成、修正、診断、高度な推論、プラン策定、一般的な質問、対話、自然な日本語文章の作成。
- deepseek-r1-distill-qwen-14b: 難解なトラブルシューティング、深い思考。
- deepseek-ocr: 画像からのテキスト抽出（OCR）。
- GPUはNVIDIAを優先的に使用し、VRAM消費を最適化する。

## 3. ソフト開発
- 本体プログラムで使用するWhisperモデルはインストール後使用する際にダウンロードを行う。
- ライブラリは本体とは別にする（python・GPU・CPU）

### エラーハンドリング (MCP Error: context deadline exceeded)
タイムアウト発生時は以下の順で対応する：
1. **チャンク分割**: 一度に読み込むファイル量やコンテキストを減らし、複数回に分けて処理する。
2. **サーバー確認**: `ollama ps` でモデルがGPU上にあり、応答可能な状態か確認する。
3. **プランの簡略化**: MCP Server側のタイムアウト制限を考慮し、重い推論が予想される場合は処理を分割・簡略化するプランを提示する。

## 3. 開発・運用プロセス
- **シンプル設計**: 200行を50行にできるなら書き直す。最小限のコード量で目的を達する。
- **確認の徹底**: 不明点は止まって聞く。確認時はプランを提示し、選択してもらう。
- **継続性**: 前回までの引き継ぎファイル（HANDOVER.md等）を必ず確認する。
- **既存機能の維持**: 修正時は既存機能を破壊しない。影響範囲が広い場合は事前警告する。
- **可観測性と安全**: 適切な日本語によるログ出力を行い、環境パスは相対パス（または指定変数）を使用してハードコーディングを避ける。
- **デプロイフロー**: 
    - 作業用: d:\temp_aurawhisper_release (rootにzip作成)
    - 保存先: D:\Users\zod5532743\マイドライブ\Antigravity\AuraWhisper\dist
- **完了定義**: 「テストが通ること」を基準とし、GitHub登録をもって終了とする。
- **引き継ぎ**: 必要に応じて日本語による HANDOVER.md の作成・更新を行う。

## 4. 設計・エラー対応指針
- **モジュール化**: コードは再利用可能な小さな単位に分割し、依存関係を明確にする。
- **原因究明 (Root Cause)**: エラー時は修正案だけでなく原因を特定し、再発防止策を含めた修正を行う。
- **整合性検証**: MCPサーバー間やDifyワークフローにおいて、データの型や構造が一致しているかを常に検証する。
