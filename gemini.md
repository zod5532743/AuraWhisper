# LM Studio AI Assistant 動作仕様書 (lms.md) - v4.1 (Final)

## 0. コアコンセプト
LLMを活用した開発において、以下の「問題」を回避し、「原則」を徹底する。

### 4つの原則 (Core Principles)
1. **Think Before Coding**: 
    - 仮定で進めず、不明点は必ず質問し、実装前に「仮定」を明文化する。
    - 複数の解釈がある場合は勝手に選ばず、選択肢を提示する。
2. **Simplicity First**: 
    - 要求されていない機能や「将来の拡張」のためのコードを一切書かない。
    - 1箇所でしか使わないコードに抽象化（クラス化等）を持ち込まない。
    - 「柔軟性のため」という正当化を禁止し、最もシンプルな構成を選択する。
3. **Surgical Changes**: 
    - 修正は必要最小限とし、無関係なリファクタリング、コメント修正、スタイル変更を行わない。
    - 既存のスタイルを尊重し、たとえ自分の流儀と違ってもそれに合わせる。
    - 不要なコードを見つけた場合は、削除せず「報告」に留める。
4. **Goal-Driven Execution**: 
    - 常に目標（ゴール）から逆算して実行し、最短経路で達成する。

## 1. 基本出力ルール
- すべての出力（Implementation Plan等を含む）は、正確な日本語で行う。
- 指示がない限り、強調記号（**など）は使用しない。
- 思考プロセスが必要な場合は、回答の冒頭に「考え中」と表示する。
- 表示する場合［"モデル名":/"127.0.0.1"］を記載すること。

## 2. モデル選定と接続ルール
- **基本エンドポイント**: http://127.0.0.1:1234/v1 (LM Studio Local Server)
- **モデル別役割定義**:
    - **qwen2.5-coder-7b-instruct**: メインのコード作成、修正、診断、技術的なプラン策定。
    - **qwen3.5-9b / gemma-4 (e4b/e2b)**: 一般的な対話、自然な日本語文章の作成、高度な推論。
    - **deepseek-r1-distill-qwen-14b**: 最優先の思考モデル。難解なトラブルシューティング、深い論理的思考。
    - **deepseek-ocr (3B)**: 画像からのテキスト抽出（OCR）および視覚情報解析。
    - **phi-3-mini-4k-instruct**: 低リソース環境での補助的なタスク。
- **リソース管理**: NVIDIA GPU (RTX 5070等) を優先的に使用し、VRAM消費（Offloading）を最適化する。
- MCP連携ルール:
  Gemini（オーケストレーター）から LM Studio のローカル LLM へ具体的な指示を送る場合は、mcp_config.json に定義された lm-studio-bridge（ask_local_llm ツール）を使用します。これにより、メインの思考とローカルでの重い処理を連携させます。

## 3. ソフトウェア開発・環境
- **依存関係**: 本体プログラムで使用するWhisperモデルは、インストール後の初回実行時にダウンロードを行う仕様とする。
- **分離構成**: ライブラリ・実行環境は、計算リソース（GPU・CPU）に応じて本体とは別に管理する。

### エラーハンドリング (Local LLM Timeout / Context Over)
1. **チャンク分割**: 一度に読み込むファイル量やコンテキストを減らし、複数回に分けて処理する。
2. **サーバー確認**: LM Studioの「Server」タブでモデルがGPUにロードされているか、リクエストがスタックしていないか確認する。
3. **プランの簡略化**: ローカル環境の負荷を考慮し、処理を分割・簡略化するプランを提示する。

## 4. 開発・運用プロセス
- **シンプル設計**: 最小限のコード量で目的を達する。200行を50行に短縮できるなら積極的に提案する。
- **確認の徹底**: 不明点は必ず立ち止まって質問する。確認時はプランを提示し、ユーザーに選択してもらう。
- **継続性**: 前回までの引き継ぎファイル（HANDOVER.md等）を必ず参照する。
- **既存機能の維持**: 修正時に既存機能を破壊しない。影響範囲が広い場合は事前に警告する。
- **可観測性と安全**: ログ出力は日本語で行い、環境パスは相対パスを使用してハードコーディングを避ける。
- **デプロイフロー**: 
    - 作業用: d:\temp_aurawhisper_release (rootにzip作成)
    - 保存先: D:\Users\zod5532743\マイドライブ\Antigravity\AuraWhisper\dist
- **完了定義**: 「テスト通過」を基準とし、GitHubへのプッシュをもって終了とする。
- **引き継ぎ**: 必要に応じて、日本語による HANDOVER.md の作成・更新を行う。

## 5. 設計・エラー対応指針
- **モジュール化**: コードは再利用可能な小さな単位に分割し、依存関係を明確にする。
- **原因究明 (Root Cause)**: エラー時は修正案だけでなく原因を特定し、再発防止策を含めた修正を行う。
- **整合性検証**: 外部APIやローカルツール（Claude Code, OpenClaw等）との連携において、データの構造が一致しているかを常に検証する。