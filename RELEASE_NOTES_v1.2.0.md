### AuraWhisper v1.2.0 リリースノート

AuraWhisperの新しいバージョン1.2.0がリリースされました。このアップデートは、使い心地の向上、機能強化、そして将来の拡張性を保証するために行われました。以下に主な変更点について詳しく解説します。

---

#### 🚀 新しい機能

1. **AI 地脈解析ダッシュボード**
   - Ollama 接続による AI 地脈分析の可視化(ON/OFFスイッチあり)
   - gemma2:2b デフォルトモデル（軽量高速推論）プルダウンメニューで他のモデルも選択可能
   - 日本語対応システムプロンプト
   - 最近の解析履歴の保存と参照

2. **"Superwhisper-like" UI リデザイン**
   - 革新的なガラスモーフィズムアートスタイル（すりガラス効果）と美しいグラデーション、心地よいマイクロアニメーションを追加しました。
   - 設定とダッシュボードのUIが劇的に向上し、よりプレミアムで洗練された体験を提供します。

2. **アプリ連動型プロンプト (App-linked Prompts)**
   - 特定のテキスト校正AIモードを、お使いのアプリケーション（例：`slack.exe`, `discord.exe`）にリンクさせることができるようになりました。
   - 録音開始時にAuraWhisperが現在アクティブなウィンドウを自動検出し、そのアプリに最適なAIプロンプトを適用して文字起こしを行います。手動でモードを切り替える手間が省けます。

3. **コンポーネント分離 & ダウンロードマネージャー**
   - 巨大なPythonバックエンド（音声認識エンジン）がElectronアプリ本体から完全に分離され、インストーラーのサイズが劇的に軽量化されました。
   - UIにオンデマンドのダウンロードマネージャーが追加され、ユーザーのハードウェア（CPU / CUDA / AMD DirectML）に合わせて最適なエンジンを初回起動時にGitHubから自動取得します。
   - ダウンロード時スプグレスバー等で周知する

4. **AMD DirectML への対応準備**
   - AMD GPU用のハードウェアスイッチが追加されました。v1.2.0では安全性を最優先し、CPU処理へのフォールバック機構を導入しています。これにより、将来的なネイティブONNXサポートへの道筋を整えました。

---

#### ⚠️ 動作前提条件と注意事項

1. Python 3.10 以降のインストールが必要です
   - PCにPythonがインストールされていない場合、アプリ起動時に警告画面が表示されます。
   - Pythonをインストールする際は、インストーラーの「Add Python to PATH」に必ずチェックを入れてください。

2. AI校正・推敲機能（Ollama）
   - AI校正機能を利用する場合は、事前にOllamaを公式サイトからインストールし、バックグラウンドで起動しておいてください。
   - デフォルトではgemma2:2bモデルなどを利用します。

3. GPU（CUDA）による高速化
   - NVIDIAのGPUを搭載している環境では、自動的にGPU（CUDA）を利用して文字起こしを高速に実行します。
   - ライブラリの読み込みに問題がある、またはGPUが非搭載の場合は、自動的にCPU処理にフォールバックして安全に動作します。

---

AuraWhisper v1.2.0 は、よりプレミアムなUI、強化された自動化機能、そしてスマートなアーキテクチャによって、Windowsでの音声入力・AI校正体験を次のレベルへと引き上げます。ぜひ最新版をお試しください！
